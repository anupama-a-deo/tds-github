name: Git-Action

on : push

jobs:
  git-action:
    runs-on: ubuntu-latest
    steps:
      - name: anupama.deo@gramene.com
        run: | #python
          import json
          file = json.loads('email.json')
          print(file['email'])
        
    
